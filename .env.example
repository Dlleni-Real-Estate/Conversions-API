# ── Meta ────────────────────────────────────────────────────────────────────
# System User token with: ads_management, ads_read, leads_retrieval,
# pages_show_list, pages_read_engagement, pages_manage_ads, business_management
META_ACCESS_TOKEN=
META_PAGE_ID=109652897854140          # Dlleni - دلني
META_DATASET_ID=1718089652564651      # "Dlleni CRM Events" — the ONLY dataset this app writes to.
                                      # It is connected to ad account 736420925136885 (dlleni ads one).
                                      # Any other id makes lib/meta.ts throw on purpose.
META_AD_ACCOUNT_ID=736420925136885    # dlleni ads one
META_API_VERSION=v23.0
# Set while testing so events land in Events Manager > Test Events only.
# REMOVE IT for production, otherwise nothing counts for optimization.
META_TEST_EVENT_CODE=

# ── Supabase ────────────────────────────────────────────────────────────────
SUPABASE_URL=https://yrmgwbufaaiaioqvabon.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# ── App ─────────────────────────────────────────────────────────────────────
APP_PASSWORD=                          # password the sales team types once
CRON_SECRET=                           # random string; Vercel sends it on cron calls

# ── 8X CRM ─────────────────────────────────────────────────────────────────
# Settings > Integrations > Manage API Keys > Generate Token.
# Scope: "Search leads" ONLY. Leaving every scope unchecked grants FULL access
# (read AND write) — the opposite of what the wording suggests.
# Note the host: the API is on 8xcrm.COM, the dashboard is on 8xcrm.NET.
CRM_API_KEY=
CRM_BASE_URL=https://dlleni.8xcrm.com
